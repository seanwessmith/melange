{
  "name": "melange-ext",
  "version": "1.0.2",
  "description": "Instant Wikipedia-style dictionary popups for any word you highlight",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "rm -rf dist && bun run builder.ts dev",
    "build": "rm -rf dist && bun run builder.ts prod",
    "release": "bun run release.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.0",
    "@types/chrome": "^0.1.24",
    "@types/node": "^24.9.1",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "bun-image-transform": "^1.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
